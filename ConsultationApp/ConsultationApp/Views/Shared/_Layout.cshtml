<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ConsultationApplication</title>
    @Styles.Render("~/Content/css")
    @Scripts.Render("~/bundles/modernizr")

</head>
<body>
    <div class="navbar navbar-inverse navbar-fixed-top">
        <div class="container">
            <div class="navbar-header">
                <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                </button>
                <img src="~/logo/Nimetu.png" alt="logo" class="navbar-brand" />
                @Html.ActionLink("Consultation App", "Index", "Consultations", new { area = "" }, new { @class = "navbar-brand" })
            </div>
            <div class="navbar-collapse collapse">
                <ul class="nav navbar-nav">
                    @*@if (User.IsInRole("Admin") || User.IsInRole("Teacher"))
                    {
                        <li>@Html.ActionLink("New Consultation", "Create", "Consultations")</li>
                    }
                    else if (!User.IsInRole("Admin") || User.IsInRole("Teacher"))
                    {
                        @Html.Hidden("New Consultation", "Create", "Consultations")
                    }

                    <li>@Html.ActionLink("Consultations", "Index", "Consultations")</li>
                    @if (User.IsInRole("Admin"))
                    {
                        <li>@Html.ActionLink("Admin-Panel", "Admin_Panel", "Consultations")</li>
                    }
                    else if (!User.IsInRole("Admin"))
                    {
                        @Html.Hidden("AdminPanel", "Admin_Panel", "Consultations")
                    }*@
                    <li>@Html.ActionLink("New Consultation", "Create", "Consultations")</li>
                     <li>@Html.ActionLink("Consultations", "Index", "Consultations")</li>
                    <li>@Html.ActionLink("Admin-Panel", "Admin_Panel", "Consultations")</li>
                </ul>
                @Html.Partial("_LoginPartial")
            </div>
        </div>
    </div>
    <div class="container body-content">
        @RenderBody()
        <hr />
        <footer>
            <p>@DateTime.Now.Day/@DateTime.Now.Month/@DateTime.Now.Year</p>
        </footer>
    </div>

    @Scripts.Render("~/bundles/jquery")
    @Scripts.Render("~/bundles/bootstrap")
    @RenderSection("scripts", required: false)
</body>
</html>
